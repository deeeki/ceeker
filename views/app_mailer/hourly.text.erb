Conversations at <%= @to_time.getlocal.strftime('%H on %b %d') %>

<% @conversations.each do |c| %>
[<%= c.created_at.getlocal.strftime('%Y/%m/%d %H:%M') %>]
  <% c.tweets.each do |t| %>
<%= t.screen_name %>
  <%= CGI.escapeHTML(t.text) %>
  <% end %>

<% end %>

--
provided by Ceeker
